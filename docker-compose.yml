services:
  memcached:
    image: memcached:latest
    ports:
      - "11211:11211"
  lsw-memcache-bundle-test:
    build:
      dockerfile: ./docker/Dockerfile
    volumes:
      - .:/app
    working_dir: /app
    command: sh -c "composer install && vendor/bin/phpunit"
